spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password: ''
  h2:
    console:
      enabled: true
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        show_sql: true
        format_sql: true

springdoc:
  swagger-ui:
    url: /swagger.yaml

build:
  version: "3.0"

accounts:
  message: "Welcome to Bank Accounts related to local APIs"
  contactDetails:
    name: "Timur Avliakulov - Java Developer"
    email: "timur@gmail.com"
  onCallSupport:
    - (123) 555-1234
    - (123) 432-6545